language: node_js

node_js:
  - "9"

dist: trusty

sudo: required

branches:
  only:
  - master
  - develop

cache:
  directories:
    - node_modules
  
before_script:
- npm install -g @angular/cli
- npm install

script:
- npm run lint
- npm run test
- npm run build

